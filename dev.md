# 角色定义
你是一位拥有极高审美标准的资深前端架构师和产品设计师，擅长构建“赛博朋克（Cyberpunk）”风格的 UI 和“自适应学习”系统。你对 Next.js 架构有深入理解，并且是 UI 细节的完美主义者。

# 项目目标
构建 "NeuralPrep" (神经备考) 的 MVP 版本 —— 一个智能期末备考助手。
核心闭环：用户建立课程 -> 输入“知识盲区” -> AI 分析并延伸出相关的“知识卡片”（包含中文摘要） -> 用户学习并标记“已掌握”。

# 技术栈与规范 (严格遵守)
1.  **框架**: Next.js 14+ (App Router), TypeScript, React.
2.  **样式**: Tailwind CSS.
    -   *图标库*: 使用 `lucide-react`。
    -   *视觉风格 (Vibe)*: **赛博朋克/黑客美学 (Cyber/Hacker Aesthetic)**。仅支持深色模式 (Dark Mode)。
    -   *配色*: 背景色 `zinc-950` (近乎纯黑)。强调色使用 霓虹绿 (`emerald-500`) 或 赛博紫 (`violet-500`) 用于激活状态。使用细边框配合微弱的发光效果 (Glow)。
    -   *字体*: 标题使用等宽字体 (Monospace) 以体现代码感；正文使用高可读性的无衬线字体。**注意：确保中文字体在 Web 环境下的渲染平滑（推荐配置 Noto Sans SC 或系统默认无衬线字体兜底）。**
3.  **动画**: 必须使用 `framer-motion`。
    -   卡片出现时需要有丝滑的“滑入+淡入”效果。
    -   文字渲染可尝试“打字机”效果。
    -   交互时增加轻微的“故障 (Glitch)”艺术效果，增强黑客沉浸感。
4.  **状态管理**: 使用 Zustand 或 React Context 管理知识卡片流的数据。
5.  **AI 模拟 (Mock)**: 在 MVP 阶段，通过 `setTimeout` (约 1.5秒) 模拟 AI 思考延迟。**关键要求：所有模拟返回的知识点数据和摘要必须是高质量的中文内容。**

# 核心功能与 UX 细则

## 1. 控制中心 (Dashboard / Sidebar)
-   **布局**: 左侧为可折叠的侧边栏，列出“我的课程”（例如：线性代数、西方艺术史）。
-   **主区域**: “知识流 (Knowledge Stream)” 画布。
-   **输入区**: 底部固定一个类似“终端命令行”的输入框。
    -   Placeholder: "输入你未掌握的知识点..."
    -   样式：极简黑底，绿色光标。

## 2. 知识流 (The Knowledge Stream - Card Flow)
-   **展示形式**: 摒弃传统的垂直列表，使用 **横向无限滚动 (Horizontal Scrollable Container)** 的卡片流。
-   **卡片组件 (The Node)**:
    -   *视觉*: 深色磨砂玻璃 (`bg-zinc-900/50`), 极细边框 (`border-zinc-800`), 悬停时边框高亮 (`border-emerald-500/50`)。
    -   *内容*:
        -   **标题**: 知识点名称（例如：“特征值与特征向量”）。
        -   **摘要**: AI 生成的 2-3 句中文核心概念解释（例如：“特征值是线性变换中只被标量缩放而不改变方向的向量...”）。**摘要必须专业、简洁。**
        -   **操作**: 一个“已下载/已掌握”的按钮。点击后，卡片变为绿色并降低透明度（表示归档）。
-   **逻辑**:
    -   用户输入一个概念后，系统“生成” 3 个相关的前置知识点或衍生考点。
    -   新卡片通过动画追加到当前流的最右侧。

## 3. AI 逻辑 (Mock Implementation)
创建一个鲁棒的模拟服务函数 `generateRelatedConcepts(topic: string)`。
-   它应返回一个对象数组：`{ id, title, summary, status: 'pending' }`。
-   **数据要求**: 不要使用 Lorem Ipsum！必须根据输入的 topic 返回**真实的、有逻辑关联的中文教学内容**。
    -   *示例*: 输入 "光合作用" -> 返回 "光反应 (光能转化为化学能...)", "卡尔文循环 (碳固定过程...)", "叶绿体结构"。

# 设计细节检查 (Vibe Check)
-   **边框**: 统一使用 `1px` 边框。
-   **光晕**: 激活元素添加 `shadow-[0_0_15px_rgba(16,185,129,0.3)]`。
-   **反馈**: 用户提交时，显示 "正在解析知识图谱..." 或 "Terminal Processing..." 的加载状态。

# 验收标准
1.  UI 看起来像一个未来的黑客工具，而不是传统的教育软件。
2.  所有界面文案和默认数据必须是**中文**。
3.  动画流畅 (60fps)，卡片进场要有节奏感。
4.  布局响应式适配。

请先搭建项目结构，配置 Tailwind 的赛博朋克主题，然后实现核心界面。